---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppetlabs/code/environments/%{::environment}/hieradata
:hierarchy:
  - node/%{::fqdn}
  - roles/%{::puppet_role}
  - environments/%{::puppet_env}
  - common
